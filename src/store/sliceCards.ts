import { createSlice } from "@reduxjs/toolkit";

export const sliceCards = createSlice({
  name: "name",
  initialState: {
    state: [],
  },
  reducers: {
    loginUser() {},
    createUser() {},
    forgotPassword() {},
  },
});
